query Voting($proposalId: Int!) {
    proposalDetail(id: $proposalId) {
        abstainVotes
        abstainVotesPercentage
        noVotes
        noVotesPercentage
        outcome
        quorum
        totalVotes
        yesVotes
        yesVotesPercentage
    }
}
