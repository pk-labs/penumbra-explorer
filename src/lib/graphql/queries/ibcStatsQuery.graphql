query IbcStats($clientId: String, $timePeriod: String) {
    ibcStats(clientId: $clientId, timePeriod: $timePeriod) {
        clientId
        shieldedVolume
        shieldedTxCount
        unshieldedVolume
        unshieldedTxCount
        pendingTxCount
        expiredTxCount
    }
}
