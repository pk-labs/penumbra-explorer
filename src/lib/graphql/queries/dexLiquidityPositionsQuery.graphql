query DexLiquidityPositions($limit: CollectionLimit!) {
    liquidityPositions(limit: $limit) {
        items {
            tradingPairAsset1
            tradingPairAsset2
            reserves1Amount
            reserves2Amount
            state
            feePercentage
            updatedAt
            positionId
        }
        total
    }
}
